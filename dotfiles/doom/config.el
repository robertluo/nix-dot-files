;;; config.el -*- lexical-binding: t; -*-

;; Read at startup, so edits here need only a restart — not a rebuild.

(setq user-full-name "Luo Tian"
      user-mail-address "lt@basecity.com")

(setq doom-theme 'doom-one)
(setq display-line-numbers-type t)
(setq org-directory "~/org/")
